# Agent instructions

- When starting a new Codex task/branch in this repo, update the branch segment of every `@require` URL in `userscript/katana-helpers.dev.user.js` to match the current Codex branch name for that task.
