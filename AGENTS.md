# Agent instructions

- When starting a new Codex task/branch in this repo, update the `DEV_BRANCH_URL` value near the top of `userscript/katana-helpers.dev.user.js` to the GitHub branch URL for that task.
